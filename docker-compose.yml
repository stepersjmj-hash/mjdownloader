services:
  reelsnap:
    build: .
    container_name: reelsnap
    # Synology Container Manager 가 dns: 옵션을 무시하는 이슈 우회.
    # 호스트 네트워크 모드로 NAS DNS/라우팅을 그대로 사용.
    network_mode: host
    restart: unless-stopped
    environment:
      - PORT=3100
